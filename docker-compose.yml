services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./public/dist:/app/public/dist
      - ./backend:/app/backend
      - ./data:/app/data
    environment:
      PYTHONPATH: /app
    env_file:
      - .env